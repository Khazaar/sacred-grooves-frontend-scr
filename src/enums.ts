export enum UserRoles {
    Artist = "Artist",
    Organizer = "Organizer",
    Visitor = "Visitor",
    SupportTeam = "SupportTeam",
}

export enum actionKeysUser {
    view = "view",
    create = "create",
    edit = "edit",
}
